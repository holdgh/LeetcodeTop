要提升千问生成模型在**专业领域问答对话**的微调效果，核心思路是 **「数据精细化+模型适配优化+训练策略调优+评估闭环」**，结合生成任务的特点和专业领域的特殊性，以下是可落地的全流程方案：

### 一、 核心前提：专业领域数据的精细化处理
数据是微调效果的基石，生成任务对数据的**格式、质量、多样性**要求极高，尤其是专业领域（如医疗、法律、金融）。
#### 1.  数据格式严格对齐千问的指令范式
千问模型的输入输出有固定格式，微调数据必须严格遵循，否则模型无法正确理解任务边界：
```
# 标准格式（必须包含user/assistant角色分隔符）
<|im_start|>user
{专业领域问题}
<|im_end|>
<|im_start|>assistant
{专业领域标准答案}
<|im_end|>
```
- **错误示例**：直接喂给模型“什么是区块链？→ 区块链是一种分布式账本技术”（无角色分隔符，模型易混淆输入输出）；
- **正确示例**：
  ```
  <|im_start|>user
  请解释金融领域中"远期合约"的定义及核心特征
  <|im_end|>
  <|im_start|>assistant
  远期合约是一种非标准化的金融衍生工具，由交易双方约定在未来某一特定时间，以预先确定的价格买卖一定数量标的资产的合约。其核心特征包括：1. 非标准化：合约条款由双方协商确定；2. 场外交易：无固定交易场所；3. 信用风险高：依赖交易对手的履约能力。
  <|im_end|>
  ```

#### 2.  数据质量把控（专业领域的核心要求）
- **标注准确性**：专业问题的答案必须由领域专家审核，避免错误信息（如医疗领域的用药建议、法律领域的条款解释）；
- **答案完整性**：生成任务的答案需要**结构化、有条理**（如分点、分层次），而非碎片化文本，方便模型学习专业表达逻辑；
- **数据去重与清洗**：删除重复问题、低质答案（如答非所问、口语化严重的内容），避免模型学到噪音；
- **数据多样性**：覆盖专业领域的**全细分场景**（如法律领域包含合同起草、纠纷解决、合规咨询等子场景），避免模型过拟合单一任务。

#### 3.  构建领域专属词表（可选，提升专业术语理解）
- 若专业领域有大量生僻术语（如生物领域的“CRISPR-Cas9”、化工领域的“催化裂化”），可扩展千问的词表，将新术语加入`vocab.json`；
- 工具：使用`sentencepiece`训练领域专属分词器，与原模型分词器融合。

### 二、 模型适配优化：针对专业生成任务的参数配置
#### 1.  优先选择 LoRA 微调（而非全量微调）
在Windows CPU环境下，全量微调千问模型（即使是0.5B）算力消耗极大，且易过拟合。**LoRA（低秩适配）** 是最优选择：
- **核心原理**：仅在Transformer的注意力层（`q_proj`/`v_proj`）插入低秩矩阵，冻结模型其他参数，训练参数量减少99%；
- **关键参数配置**（专业领域适配）：
  ```python
  from peft import LoraConfig, get_peft_model

  lora_config = LoraConfig(
      r=16,  # 秩，越大拟合能力越强，建议8~32（专业领域取16~32）
      lora_alpha=32,  # 缩放因子，通常为r的2倍
      target_modules=["q_proj", "v_proj"],  # 仅微调注意力层
      lora_dropout=0.05,  # dropout率，防止过拟合
      bias="none",
      task_type="CAUSAL_LM"  # 因果语言模型（生成任务）
  )
  model = get_peft_model(model, lora_config)
  ```

#### 2.  量化感知微调（QAT）：兼顾压缩与精度
如果需要在量化后的模型上做专业领域微调，建议使用 **量化感知微调**：
- 先对原始模型做4bit/GPTQ量化，再在量化模型上做LoRA微调；
- 核心优势：微调后的模型既保留专业领域能力，又能在CPU上高效推理；
- 工具：使用`auto_gptq`+`peft`联动，代码示例：
  ```python
  from auto_gptq import AutoGPTQForCausalLM
  from peft import LoraConfig, get_peft_model

  # 加载量化模型
  model = AutoGPTQForCausalLM.from_quantized(
      "./qwen2.5-0.5B-gptq-4bit",
      device_map="cpu",
      use_triton=False,
      trust_remote_code=True
  )
  # 配置LoRA并微调
  lora_config = LoraConfig(r=16, target_modules=["q_proj", "v_proj"])
  model = get_peft_model(model, lora_config)
  ```

### 三、 训练策略调优：适配生成任务的关键技巧
#### 1.  损失函数与超参数优化
- **损失函数**：生成任务优先使用 **交叉熵损失**，并加入 **标签平滑**（`label_smoothing_factor=0.1`），提升模型泛化能力；
- **学习率**：LoRA微调的学习率建议取 `1e-4 ~ 5e-4`（专业领域数据少，可适当提高到`5e-4`），全量微调取`1e-5`；
- **批次大小**：Windows CPU环境下，`per_device_train_batch_size=1`，并开启 **梯度累积**（`gradient_accumulation_steps=4`），模拟大批次训练效果；
- **训练轮数**：专业领域数据量通常较小（1000~5000条），训练轮数建议 `3~5` 轮，避免过拟合（过拟合表现为：模型只背答案，无法回答变体问题）。

#### 2.  输入输出长度控制
- **最大序列长度**：根据专业领域问题的长度设置，如法律问题较长，可设`max_seq_len=512`，避免截断关键信息；
- **生成参数控制**：微调后的推理阶段，设置合理的生成参数：
  ```python
  outputs = model.generate(
      **inputs,
      max_new_tokens=200,  # 控制答案长度
      temperature=0.7,  # 0.1~1.0，越低越精准，越高越有创造性
      top_p=0.9,  # 核采样，避免生成无关内容
      repetition_penalty=1.1,  # 重复惩罚，防止专业术语重复
      do_sample=True
  )
  ```

#### 3.  加入领域知识蒸馏（进阶技巧）
如果有专业领域的大模型（如ChatGLM-6B医疗版），可以用**蒸馏**的方式，将大模型的专业知识迁移到千问小模型：
- 以专业大模型为**教师模型**，千问为**学生模型**；
- 用专业领域数据生成教师模型的软标签（温度系数`T=5`）；
- 采用我们之前的蒸馏损失函数，让千问学习教师模型的专业知识分布。

### 四、 评估闭环：验证专业领域微调效果
微调效果不能只靠“人工看”，需要建立量化评估指标+人工评估的闭环。
#### 1.  量化评估指标
生成任务的核心评估指标：
- **困惑度（Perplexity, PPL）**：越低越好，反映模型对专业数据的拟合程度（PPL<10表示拟合良好）；
- **BLEU/Rouge-L**：衡量生成答案与标准答案的相似度（专业领域更关注Rouge-L，衡量语义一致性）。

#### 2.  人工评估维度
针对专业领域问答，人工评估需覆盖以下维度：
| 评估维度       | 检查内容                                                                 |
|----------------|--------------------------------------------------------------------------|
| 准确性         | 答案是否符合专业知识，有无错误（如医疗禁忌、法律条款）|
| 完整性         | 是否覆盖问题的核心要点，有无遗漏关键信息                                 |
| 流畅性         | 表达是否符合专业话术，有无口语化、歧义表述                               |
| 泛化能力       | 能否回答问题的变体（如“解释远期合约”→“远期合约和期货的区别”）|

### 五、 避坑指南：专业领域微调常见问题
1. **模型“背答案”不理解问题**：原因是数据量少+训练轮数过多，解决方案：增加数据多样性+减少训练轮数+加入标签平滑；
2. **生成内容包含错误专业术语**：原因是数据标注不准确，解决方案：由领域专家审核数据+加入术语词典，强制模型学习正确术语；
3. **CPU训练速度过慢**：解决方案：使用LoRA微调+梯度累积+量化模型微调，避免全量微调。

---

# 千问专业领域微调的标准化项目模板

你需要的千问大模型**专业领域微调标准化项目模板**如下，涵盖「数据预处理→LoRA微调→推理评估→模型部署」全流程，适配法律/医疗/金融等专业领域，且完全兼容Windows CPU环境：

```
# 千问专业领域微调项目模板（标准化结构）
qwen_domain_finetune/
├── data/                      # 数据目录
│   ├── raw/                   # 原始专业数据（如json/csv）
│   │   └── finance_data.json  # 示例：金融领域原始数据
│   ├── processed/             # 预处理后的数据
│   │   └── train.json         # 训练集（80%）
│   │   └── dev.json           # 验证集（20%）
│   └── eval/                  # 评估用测试集
│       └── test.json
├── config/                    # 配置文件
│   └── finetune_config.py     # 微调超参数配置
├── src/                       # 核心代码
│   ├── data_processor.py      # 数据预处理
│   ├── model_wrapper.py       # 模型加载+LoRA配置
│   ├── trainer.py             # 自定义训练器
│   ├── evaluator.py           # 效果评估
│   └── inferencer.py          # 推理部署
├── output/                    # 输出目录
│   ├── model/                 # 微调后的模型
│   └── logs/                  # 训练日志
├── run_finetune.py            # 微调主脚本
├── run_infer.py               # 推理主脚本
└── requirements.txt           # 依赖清单
```

## 一、 核心文件代码实现
### 1. requirements.txt（环境依赖）
```txt
torch==2.1.0
transformers==4.38.2
datasets==2.16.1
peft==0.8.2
accelerate==0.27.0
evaluate==0.4.1
rouge_score==0.1.2
sentencepiece==0.1.99
numpy==1.26.4
```

### 2. config/finetune_config.py（标准化配置）
```python
"""专业领域微调超参数配置（金融/医疗/法律通用）"""
import os

# 基础路径
BASE_PATH = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
DATA_PATH = os.path.join(BASE_PATH, "data")
OUTPUT_PATH = os.path.join(BASE_PATH, "output")
MODEL_PATH = "Qwen/Qwen2.5-0.5B-Instruct"  # 基础模型

# 数据配置
TRAIN_DATA = os.path.join(DATA_PATH, "processed/train.json")
DEV_DATA = os.path.join(DATA_PATH, "processed/dev.json")
TEST_DATA = os.path.join(DATA_PATH, "eval/test.json")
MAX_SEQ_LEN = 512  # 专业领域建议512（覆盖长问题）
TRAIN_SIZE = 0.8   # 训练集比例

# LoRA配置（专业领域最优参数）
LORA_CONFIG = {
    "r": 16,                # 秩，专业领域建议16~32
    "lora_alpha": 32,       # 缩放因子，通常为r的2倍
    "target_modules": ["q_proj", "v_proj"],  # 仅微调注意力层
    "lora_dropout": 0.05,   # 防止过拟合
    "bias": "none",
    "task_type": "CAUSAL_LM"
}

# 训练配置（CPU适配）
TRAIN_CONFIG = {
    "per_device_train_batch_size": 1,
    "per_device_eval_batch_size": 1,
    "gradient_accumulation_steps": 4,  # 模拟大批次
    "learning_rate": 5e-4,             # 专业领域LoRA建议5e-4
    "num_train_epochs": 3,             # 避免过拟合
    "max_steps": -1,
    "logging_steps": 5,
    "save_steps": 100,
    "evaluation_strategy": "epoch",    # 每轮验证
    "save_strategy": "epoch",
    "fp16": False,                     # CPU关闭FP16
    "bf16": False,
    "weight_decay": 0.01,
    "label_smoothing_factor": 0.1,     # 提升泛化
    "output_dir": os.path.join(OUTPUT_PATH, "model"),
    "report_to": "none",
    "load_best_model_at_end": True     # 保存最优模型
}

# 推理配置
INFER_CONFIG = {
    "max_new_tokens": 200,
    "temperature": 0.7,    # 专业领域0.5~0.8（平衡精准与灵活）
    "top_p": 0.9,
    "repetition_penalty": 1.1,
    "do_sample": True,
    "pad_token_id": None   # 自动匹配模型pad_token
}
```

### 3. src/data_processor.py（数据预处理）
```python
"""专业领域数据预处理：格式标准化+划分训练/验证集"""
import json
import random
from sklearn.model_selection import train_test_split
from config.finetune_config import DATA_PATH, TRAIN_SIZE, MAX_SEQ_LEN

class QwenDomainDataProcessor:
    def __init__(self, domain="finance"):
        self.domain = domain
        self.qwen_prompt_template = """<|im_start|>user
{question}
<|im_end|>
<|im_start|>assistant
{answer}
<|im_end|>"""

    def load_raw_data(self, raw_data_path):
        """加载原始数据（支持json格式）"""
        with open(raw_data_path, "r", encoding="utf-8") as f:
            raw_data = json.load(f)
        return raw_data

    def format_data(self, raw_data):
        """标准化为千问指令格式"""
        formatted_data = []
        for item in raw_data:
            # 适配不同数据格式（question/query, answer/response）
            question = item.get("question") or item.get("query")
            answer = item.get("answer") or item.get("response")
            
            if not question or not answer:
                continue
            
            # 生成千问格式文本
            formatted_text = self.qwen_prompt_template.format(
                question=question.strip(),
                answer=answer.strip()
            )
            formatted_data.append({"text": formatted_text})
        return formatted_data

    def split_train_dev(self, formatted_data):
        """划分训练/验证集"""
        train_data, dev_data = train_test_split(
            formatted_data,
            train_size=TRAIN_SIZE,
            random_state=42  # 固定随机种子
        )
        return train_data, dev_data

    def save_processed_data(self, data, save_path):
        """保存预处理后的数据"""
        with open(save_path, "w", encoding="utf-8") as f:
            json.dump(data, f, ensure_ascii=False, indent=4)

    def run(self, raw_data_path):
        """完整预处理流程"""
        # 1. 加载原始数据
        raw_data = self.load_raw_data(raw_data_path)
        print(f"加载原始数据：{len(raw_data)}条")
        
        # 2. 格式标准化
        formatted_data = self.format_data(raw_data)
        print(f"格式化后数据：{len(formatted_data)}条")
        
        # 3. 划分训练/验证集
        train_data, dev_data = self.split_train_dev(formatted_data)
        print(f"训练集：{len(train_data)}条，验证集：{len(dev_data)}条")
        
        # 4. 保存数据
        self.save_processed_data(train_data, f"{DATA_PATH}/processed/train.json")
        self.save_processed_data(dev_data, f"{DATA_PATH}/processed/dev.json")
        print("数据预处理完成！")

if __name__ == "__main__":
    # 示例：金融领域数据预处理
    processor = QwenDomainDataProcessor(domain="finance")
    processor.run(f"{DATA_PATH}/raw/finance_data.json")
```

### 4. src/model_wrapper.py（模型加载+LoRA配置）
```python
"""模型加载+LoRA配置：适配专业领域微调"""
from transformers import AutoModelForCausalLM, AutoTokenizer
from peft import LoraConfig, get_peft_model, prepare_model_for_kbit_training
from config.finetune_config import MODEL_PATH, LORA_CONFIG

class QwenDomainModelWrapper:
    def __init__(self, use_quantization=False):
        self.use_quantization = use_quantization  # 是否使用量化模型
        self.tokenizer = self._load_tokenizer()
        self.model = self._load_model()
        self.model = self._configure_lora(self.model)

    def _load_tokenizer(self):
        """加载分词器"""
        tokenizer = AutoTokenizer.from_pretrained(
            MODEL_PATH,
            trust_remote_code=True,
            padding_side="right"  # 生成任务必须右padding
        )
        tokenizer.pad_token = tokenizer.eos_token
        tokenizer.pad_token_id = tokenizer.eos_token_id
        return tokenizer

    def _load_model(self):
        """加载基础模型（支持量化）"""
        model_kwargs = {
            "trust_remote_code": True,
            "torch_dtype": "float32",
            "device_map": "cpu",
            "low_cpu_mem_usage": True,
            "use_cache": False  # 训练时关闭cache
        }

        # 量化模型加载（可选）
        if self.use_quantization:
            from auto_gptq import AutoGPTQForCausalLM
            model = AutoGPTQForCausalLM.from_quantized(
                "./output/model/qwen2.5-0.5B-gptq-4bit",
                **model_kwargs
            )
            model = prepare_model_for_kbit_training(model)
        else:
            model = AutoModelForCausalLM.from_pretrained(MODEL_PATH, **model_kwargs)
        
        return model

    def _configure_lora(self, model):
        """配置LoRA"""
        lora_config = LoraConfig(**LORA_CONFIG)
        model = get_peft_model(model, lora_config)
        # 打印可训练参数
        model.print_trainable_parameters()
        return model

    def get_model_tokenizer(self):
        """返回模型和分词器"""
        return self.model, self.tokenizer

if __name__ == "__main__":
    # 测试模型加载
    wrapper = QwenDomainModelWrapper(use_quantization=False)
    model, tokenizer = wrapper.get_model_tokenizer()
    print("模型加载完成！")
```

### 5. src/trainer.py（自定义训练器）
```python
"""专业领域微调训练器：适配LoRA+CPU"""
from transformers import Trainer, TrainingArguments, DataCollatorForLanguageModeling
from datasets import Dataset
import json
from config.finetune_config import TRAIN_CONFIG, TRAIN_DATA, DEV_DATA

class QwenDomainTrainer(Trainer):
    """自定义训练器：适配专业领域生成任务"""
    def __init__(self, tokenizer, **kwargs):
        super().__init__(**kwargs)
        self.tokenizer = tokenizer

    def compute_loss(self, model, inputs, return_outputs=False):
        """重写损失计算：适配生成任务"""
        outputs = model(
            input_ids=inputs["input_ids"],
            attention_mask=inputs["attention_mask"],
            labels=inputs["input_ids"]  # 生成任务用input_ids做labels
        )
        loss = outputs.loss
        return (loss, outputs) if return_outputs else loss

def load_dataset(data_path, tokenizer, max_seq_len):
    """加载并编码数据集"""
    with open(data_path, "r", encoding="utf-8") as f:
        data = json.load(f)
    dataset = Dataset.from_list(data)
    
    # 编码函数
    def encode_fn(examples):
        return tokenizer(
            examples["text"],
            max_length=max_seq_len,
            truncation=True,
            padding="max_length",
            return_tensors="pt"
        )
    
    encoded_dataset = dataset.map(encode_fn, batched=True)
    encoded_dataset.set_format(type="torch", columns=["input_ids", "attention_mask"])
    return encoded_dataset

def run_training(model, tokenizer):
    """启动训练"""
    # 1. 加载数据集
    train_dataset = load_dataset(TRAIN_DATA, tokenizer, max_seq_len=MAX_SEQ_LEN)
    dev_dataset = load_dataset(DEV_DATA, tokenizer, max_seq_len=MAX_SEQ_LEN)
    
    # 2. 数据整理器
    data_collator = DataCollatorForLanguageModeling(
        tokenizer=tokenizer,
        mlm=False  # 生成任务关闭MLM
    )
    
    # 3. 训练参数
    training_args = TrainingArguments(**TRAIN_CONFIG)
    
    # 4. 初始化训练器
    trainer = QwenDomainTrainer(
        model=model,
        args=training_args,
        train_dataset=train_dataset,
        eval_dataset=dev_dataset,
        data_collator=data_collator,
        tokenizer=tokenizer
    )
    
    # 5. 开始训练
    print("启动专业领域微调...")
    trainer.train()
    
    # 6. 保存模型
    trainer.save_model(TRAIN_CONFIG["output_dir"])
    tokenizer.save_pretrained(TRAIN_CONFIG["output_dir"])
    print("微调完成，模型已保存！")

if __name__ == "__main__":
    from model_wrapper import QwenDomainModelWrapper
    wrapper = QwenDomainModelWrapper()
    model, tokenizer = wrapper.get_model_tokenizer()
    run_training(model, tokenizer)
```

### 6. src/evaluator.py（效果评估）
```python
"""专业领域微调效果评估：PPL+Rouge-L"""
from transformers import AutoModelForCausalLM, AutoTokenizer, PerplexityScore
from datasets import Dataset
import evaluate
import json
import torch
from config.finetune_config import TEST_DATA, INFER_CONFIG

class QwenDomainEvaluator:
    def __init__(self, model_path):
        self.model_path = model_path
        self.tokenizer = AutoTokenizer.from_pretrained(model_path, trust_remote_code=True)
        self.model = AutoModelForCausalLM.from_pretrained(
            model_path,
            trust_remote_code=True,
            device_map="cpu",
            torch_dtype=torch.float32
        )
        # 加载评估指标
        self.perplexity = evaluate.load("perplexity")
        self.rouge = evaluate.load("rouge")

    def load_test_data(self):
        """加载测试集（包含question+answer）"""
        with open(TEST_DATA, "r", encoding="utf-8") as f:
            test_data = json.load(f)
        # 解析question和answer
        parsed_data = []
        for item in test_data:
            text = item["text"]
            # 提取user和assistant内容
            user_start = text.find("<|im_start|>user\n") + len("<|im_start|>user\n")
            user_end = text.find("\n<|im_end|>")
            assistant_start = text.find("<|im_start|>assistant\n") + len("<|im_start|>assistant\n")
            assistant_end = text.find("\n<|im_end|>", assistant_start)
            
            question = text[user_start:user_end].strip()
            reference = text[assistant_start:assistant_end].strip()
            
            parsed_data.append({
                "question": question,
                "reference": reference
            })
        return parsed_data

    def compute_perplexity(self, test_texts):
        """计算困惑度PPL"""
        results = self.perplexity.compute(
            predictions=test_texts,
            model_id=self.model_path,
            device="cpu",
            max_length=MAX_SEQ_LEN,
            truncate=True
        )
        avg_ppl = sum(results["perplexities"]) / len(results["perplexities"])
        print(f"平均困惑度（PPL）：{avg_ppl:.2f}")
        return avg_ppl

    def generate_answers(self, parsed_data):
        """生成测试集答案"""
        predictions = []
        references = []
        
        for item in parsed_data:
            # 构建输入
            inputs = self.tokenizer(
                f"<|im_start|>user\n{item['question']}\n<|im_end|>\n<|im_start|>assistant\n",
                return_tensors="pt"
            )
            # 生成答案
            outputs = self.model.generate(
                **inputs,
                max_new_tokens=INFER_CONFIG["max_new_tokens"],
                temperature=INFER_CONFIG["temperature"],
                top_p=INFER_CONFIG["top_p"],
                repetition_penalty=INFER_CONFIG["repetition_penalty"],
                do_sample=INFER_CONFIG["do_sample"],
                pad_token_id=self.tokenizer.pad_token_id
            )
            # 解析生成结果
            pred = self.tokenizer.decode(outputs[0], skip_special_tokens=True)
            pred = pred.split("assistant\n")[-1].strip()
            
            predictions.append(pred)
            references.append(item["reference"])
        
        return predictions, references

    def compute_rouge(self, predictions, references):
        """计算Rouge-L"""
        results = self.rouge.compute(
            predictions=predictions,
            references=references,
            rouge_types=["rougeL"]
        )
        rouge_l = results["rougeL"] * 100
        print(f"Rouge-L得分：{rouge_l:.2f}%")
        return rouge_l

    def run_evaluation(self):
        """完整评估流程"""
        print("加载测试集...")
        parsed_data = self.load_test_data()
        
        # 1. 计算PPL
        test_texts = [item["reference"] for item in parsed_data]
        self.compute_perplexity(test_texts)
        
        # 2. 生成答案并计算Rouge-L
        print("生成测试集答案...")
        predictions, references = self.generate_answers(parsed_data)
        self.compute_rouge(predictions, references)
        
        # 3. 输出部分示例
        print("\n=== 评估示例 ===")
        for i in range(3):
            print(f"问题：{parsed_data[i]['question']}")
            print(f"标准答案：{references[i][:50]}...")
            print(f"生成答案：{predictions[i][:50]}...")
            print("-" * 50)

if __name__ == "__main__":
    evaluator = QwenDomainEvaluator("./output/model")
    evaluator.run_evaluation()
```

### 7. src/inferencer.py（推理部署）
```python
"""专业领域推理部署：标准化接口"""
from transformers import AutoModelForCausalLM, AutoTokenizer
import torch
from config.finetune_config import INFER_CONFIG, OUTPUT_PATH

class QwenDomainInferencer:
    def __init__(self, model_path=None):
        self.model_path = model_path or f"{OUTPUT_PATH}/model"
        # 加载模型和分词器
        self.tokenizer = AutoTokenizer.from_pretrained(
            self.model_path,
            trust_remote_code=True
        )
        self.tokenizer.pad_token = self.tokenizer.eos_token
        self.model = AutoModelForCausalLM.from_pretrained(
            self.model_path,
            trust_remote_code=True,
            device_map="cpu",
            torch_dtype=torch.float32,
            low_cpu_mem_usage=True
        )
        # 生成配置
        self.infer_config = INFER_CONFIG
        self.infer_config["pad_token_id"] = self.tokenizer.pad_token_id

    def generate(self, question):
        """专业领域问答生成"""
        # 构建千问格式输入
        prompt = f"<|im_start|>user\n{question}\n<|im_end|>\n<|im_start|>assistant\n"
        
        # 编码
        inputs = self.tokenizer(
            prompt,
            return_tensors="pt",
            padding=True,
            truncation=True,
            max_length=MAX_SEQ_LEN
        )
        
        # 生成答案
        with torch.no_grad():
            outputs = self.model.generate(
                **inputs,
                **self.infer_config
            )
        
        # 解析结果
        answer = self.tokenizer.decode(outputs[0], skip_special_tokens=True)
        answer = answer.split("assistant\n")[-1].strip()
        return answer

    def batch_generate(self, questions):
        """批量生成"""
        answers = []
        for q in questions:
            ans = self.generate(q)
            answers.append(ans)
        return answers

if __name__ == "__main__":
    # 示例：金融领域推理
    inferencer = QwenDomainInferencer()
    # 专业问题示例
    question = "请解释金融领域中远期合约的信用风险特征"
    answer = inferencer.generate(question)
    print(f"问题：{question}")
    print(f"答案：{answer}")
```

### 8. run_finetune.py（微调主脚本）
```python
"""千问专业领域微调主脚本：一键运行"""
from src.data_processor import QwenDomainDataProcessor
from src.model_wrapper import QwenDomainModelWrapper
from src.trainer import run_training
from config.finetune_config import DATA_PATH

def main():
    # 1. 数据预处理
    print("===== 1. 数据预处理 =====")
    processor = QwenDomainDataProcessor(domain="finance")
    processor.run(f"{DATA_PATH}/raw/finance_data.json")
    
    # 2. 加载模型+配置LoRA
    print("\n===== 2. 加载模型 =====")
    wrapper = QwenDomainModelWrapper(use_quantization=False)
    model, tokenizer = wrapper.get_model_tokenizer()
    
    # 3. 启动微调
    print("\n===== 3. 启动微调 =====")
    run_training(model, tokenizer)
    
    print("\n===== 微调完成！=====")

if __name__ == "__main__":
    main()
```

### 9. run_infer.py（推理主脚本）
```python
"""千问专业领域推理主脚本"""
from src.inferencer import QwenDomainInferencer

def main():
    # 初始化推理器
    inferencer = QwenDomainInferencer()
    
    # 交互式问答
    print("===== 千问专业领域问答系统 =====")
    print("输入'quit'退出")
    while True:
        question = input("\n请输入专业问题：")
        if question.lower() == "quit":
            break
        answer = inferencer.generate(question)
        print(f"回答：{answer}")

if __name__ == "__main__":
    main()
```

## 二、 模板使用指南
### 1. 环境准备
```bash
# 安装依赖
pip install -r requirements.txt
# 创建目录
mkdir -p data/raw data/processed data/eval output/model output/logs
```

### 2. 数据准备
- 将专业领域数据按以下格式保存为`data/raw/finance_data.json`（以金融为例）：
  ```json
  [
      {
          "question": "什么是远期合约？",
          "answer": "远期合约是一种非标准化的金融衍生工具..."
      },
      {
          "question": "远期合约和期货合约的区别？",
          "answer": "1. 标准化程度不同...2. 交易场所不同..."
      }
  ]
  ```

### 3. 一键微调
```bash
python run_finetune.py
```

### 4. 推理测试
```bash
python run_infer.py
```

### 5. 效果评估
```bash
# 先将测试集保存为data/eval/test.json
python src/evaluator.py
```

## 三、 专业领域适配技巧
### 1. 金融领域
- LoRA参数：`r=32, lora_alpha=64`，学习率`5e-4`；
- 生成参数：`temperature=0.6, repetition_penalty=1.2`（避免术语重复）；
- 评估重点：Rouge-L（语义一致性）> PPL。

### 2. 医疗领域
- LoRA参数：`r=24, lora_alpha=48`，学习率`4e-4`；
- 生成参数：`temperature=0.5, top_p=0.85`（优先准确性）；
- 评估重点：人工审核答案准确性 > 量化指标。

### 3. 法律领域
- LoRA参数：`r=16, lora_alpha=32`，学习率`3e-4`；
- 生成参数：`temperature=0.7, max_new_tokens=300`（支持长答案）；
- 评估重点：条款准确性 + 逻辑完整性。

## 四、 核心优势
1. **标准化结构**：按工业级项目组织代码，便于维护和扩展；
2. **CPU友好**：所有配置适配Windows CPU，无需GPU；
3. **领域适配**：针对专业领域优化LoRA/生成参数，兼顾精准度和泛化性；
4. **全流程闭环**：数据预处理→微调→评估→推理全覆盖；
5. **低代码修改**：仅需替换数据和调整少量参数，即可适配不同专业领域。

## 总结
1. 这份模板是千问专业领域微调的**标准化解决方案**，覆盖从数据到部署的全流程；
2. 核心优化点：LoRA低秩适配（减少训练参数量）+ 指令格式标准化（提升模型理解）+ 量化评估（验证微调效果）；
3. 适配不同专业领域仅需：替换数据 + 调整LoRA/生成参数 + 补充领域评估维度。

使用时优先保证数据质量（专业专家审核），再通过调整LoRA秩和学习率优化效果，最终通过PPL+Rouge-L+人工评估验证微调效果。